# =============================================================================
# vLLM推理服务压测工具依赖包
# =============================================================================
# 安装命令: pip install -r requirements.txt

# -----------------------------------------------------------------------------
# 核心依赖包
# -----------------------------------------------------------------------------

# vLLM推理引擎 - 用于分词器和工具函数
# 注意：这里使用的是海光DCU K100-AI(DTK25.04)优化版本
vllm==0.6.2+das.opt3.dtk2504

# HuggingFace变换器库 - 提供分词器和模型工具
transformers==4.45.0

# 异步HTTP客户端 - 用于发送压测请求到vLLM服务器
aiohttp==3.11.18

# 数据处理和分析库 - 用于结果聚合、统计分析和CSV生成
pandas==2.1.3

# HuggingFace数据集库 - 用于加载各种测试数据集
datasets==2.14.1

# -----------------------------------------------------------------------------
# 可选依赖包（根据需要取消注释）
# -----------------------------------------------------------------------------

# 数据可视化库（用于生成性能图表）
# matplotlib==3.7.0
# seaborn==0.11.0
# plotly==5.0.0

# 数值计算库（通常由其他包自动安装）
# numpy==1.25.0

# 其他工具库
# pyyaml>=6.0            # YAML配置文件解析
# tqdm>=4.64.0           # 进度条显示
# pillow>=8.0.0          # 图像处理（多模态测试需要）
# soundfile>=0.12.0      # 音频处理（语音测试需要）

# -----------------------------------------------------------------------------
# 安装说明
# -----------------------------------------------------------------------------
# 1. 标准安装：pip install -r requirements.txt
# 2. 如果使用标准vLLM版本，请将第一行改为：vllm>=0.6.0
# 3. 根据实际需要调整版本号
# 4. 在虚拟环境中安装以避免依赖冲突